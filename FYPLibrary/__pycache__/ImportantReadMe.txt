After any edit to the files within this FYPLibrary file, ensure that
the compiled pyc file has been updated to reflect the changes.
This is done by navigating into the FYPLibrary folder (using cd ...),
and then running the following command:

python -m compileall